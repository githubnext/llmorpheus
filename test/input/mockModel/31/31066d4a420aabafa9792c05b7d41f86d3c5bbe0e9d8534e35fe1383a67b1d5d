["\n\nOption 1: The PLACEHOLDER should be replaced with:\n```\ngetOffsetStr(utcOffset)\n```\nOption 2: The PLACEHOLDER should be replaced with:\n```\n`${utcOffset > 0 ? '+' : '-'}${getNumStr(Math.abs(utcOffset) / 60)}:${getNumStr(Math.abs(utcOffset) % 60)}`\n```\nOption 3: The PLACEHOLDER should be replaced with:\n```\n`UTC${utcOffset > 0 ? '+' : '-'}${getNumStr(Math.abs(utcOffset) / 60)}:${getNumStr(Math.abs(utcOffset) % 60)}`\n```\n","\n\nOption 1: The PLACEHOLDER should be replaced with:\n```\ngetOffsetStr(utcOffset)\n```\nOption 2: The PLACEHOLDER should be replaced with:\n```\ngetNumStr(utcOffset)\n```\nOption 3: The PLACEHOLDER should be replaced with:\n```\n`${utcOffset}`\n```\n"]