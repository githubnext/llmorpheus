["\n\nOption 1: The PLACEHOLDER should be replaced with:\n```\ndeliverTimezones(timezones, options)\n```\nOption 2: The PLACEHOLDER should be replaced with:\n```\nObject.values(timezones)\n```\nOption 3: The PLACEHOLDER should be replaced with:\n```\nObject.keys(timezones).map(key => timezones[key])\n```\nOption 4: The PLACEHOLDER should be replaced with:\n```\nObject.values(timezones).filter(tz => !tz.deprecated)\n```\nOption 5: The PLACEHOLDER should be replaced with:\n```\nObject.keys(timezones).reduce((prev, key) => {\n  if (!timezones[key].deprecated) prev[key] = timezones[key];\n  return prev;\n}, {})\n```\n"]